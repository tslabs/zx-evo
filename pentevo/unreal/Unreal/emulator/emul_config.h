#pragma once
#include <vector>
#include <Poco/Util/IniFileConfiguration.h>
/*
struct emul_config
{


};

static void alt_load_config(emul_config &config, const std::string &fname) 
{
	Poco::Util::IniFileConfiguration config(fname);
	std::vector<std::string> keys{};
	config.keys(keys);
}*/
