
To get Python test working:
1. Run Zadig.
2. Select Single RS232-HS (List all devices).
3. Install libusb-win32 driver.

To restore driver which supports C++ libs, uninstall device removing drivers, and install FTDIBUS driver (https://ftdichip.com/drivers/d2xx-drivers/).
